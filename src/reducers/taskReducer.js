import {
  DRINK_COFFEE, 
  EAT_SNACK, 
  TAKE_NAP, 
  STUDY
} from '../actions/taskActions';

export default function reducer(state, action) {
  switch(action.type) {
    default: 
      return state; 
  }
}
